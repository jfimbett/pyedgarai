# pytest.ini configuration
[tool:pytest]
addopts = -v --tb=short -m "not integration"
markers =
    integration: marks tests as integration tests (may be slow and require API access)
    slow: marks tests as slow
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
